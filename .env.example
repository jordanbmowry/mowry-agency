# Email Configuration
# For Gmail, use smtp.gmail.com with port 587
# You'll need to create an "App Password" in your Google Account settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Agency email where leads will be sent
AGENCY_EMAIL=example@gmail.com
AGENCY_PHONE="(111) 222-3333"
AGENCY_ADDRESS="123 Main St, Anytown, USA"

# Supabase Configuration - NEW PROJECT (Nuxt Module Format)
SUPABASE_URL=your-supabase-project-url
SUPABASE_ANON_KEY=your-supabase-anon-key
# If your project uses SUPABASE_KEY instead, rename this variable accordingly in your code and here.

# Supabase Secret Key (recommended for server-side operations that bypass RLS)
SUPABASE_SECRET_KEY=your-supabase-secret-key

# Example for other email providers:
# For Outlook: smtp-mail.outlook.com, port 587
# For Yahoo: smtp.mail.yahoo.com, port 587
# For custom SMTP: your-smtp-server.com, port 587 or 465