[build]
  command = "npm rebuild better-sqlite3 && npm run build"
  publish = ".output/public"

[[functions]]
  # Enable server-side routes for API endpoints
  directory = ".output/server"

[build.environment]
  NODE_VERSION = "20"

[context.production.environment]
  NODE_ENV = "production"